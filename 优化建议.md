# 占星骰子项目 - 后续优化功能建议

## 📋 概述

基于对现有代码的深入分析，以下是针对占星骰子项目的后续优化建议，按优先级和实现难度分类，帮助项目持续迭代和功能增强。

---

## 🔴 P0 - 高优先级（核心体验优化）

### 1. 数据备份与恢复功能

**功能描述**：
- 支持导出所有数据为 JSON 文件
- 支持从 JSON 文件导入数据
- 提供一键清空数据（带二次确认）

**实现要点**：
```typescript
// 建议添加到 useDiceStore
exportData: () => string;
importData: (data: string) => { success: boolean; error?: string };
```

**预期收益**：
- 用户数据安全保障
- 支持设备间迁移
- 防止误操作导致数据丢失

---

### 2. 解读生成算法优化

**当前问题**：
- 目前解读是基于静态映射表，432种组合覆盖率不足
- 缺乏对整体组合的动态解读能力

**优化方案**：
- 基于行星能量、星座元素、宫位领域的动态解读算法
- 引入相位关系（行星与星座的守护、擢升、落陷等）
- 根据问题类型调整解读侧重点

**核心算法思路**：
```typescript
interface PlanetStrength {
  planet: string;
  sign: string;
  dignity: 'ruler' | 'exaltation' | 'detriment' | 'fall' | 'neutral';
  score: number;
}

function calculatePlanetStrength(planet: string, sign: string): PlanetStrength {
  // 计算行星在该星座的力量
  // 如：太阳在白羊座 = 擢升，太阳在天秤座 = 落陷
}

function generateDynamicInterpretation(result: DiceResult, questionType: string) {
  // 1. 计算各行星力量
  // 2. 分析元素平衡
  // 3. 分析模式分布
  // 4. 结合问题类型生成个性化解读
}
```

---

### 3. 性能优化：虚拟滚动

**问题场景**：
- 当记录数量超过100条时，列表渲染可能卡顿
- 收藏集详情页同样存在此问题

**优化方案**：
- 使用 `react-window` 或 `@tanstack/react-virtual` 实现虚拟滚动
- 只渲染可视区域内的记录卡片

**预期收益**：
- 即使有1000+条记录也保持流畅
- 降低内存占用

---

## 🟠 P1 - 中优先级（功能增强）

### 4. 标签系统增强

**功能描述**：
- 支持为单条记录添加多个自定义标签
- 支持按标签筛选记录
- 标签管理（增删改、颜色自定义）

**数据模型扩展**：
```typescript
interface Tag {
  id: string;
  name: string;
  color: string;
}

interface DiceRecord {
  // ... 现有字段
  tags: string[];  // 标签ID数组
}
```

**相关文件修改**：
- [types/index.ts](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/types/index.ts) - 扩展类型定义
- [store/useDiceStore.ts](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/store/useDiceStore.ts) - 添加标签管理方法
- [components/RecordCard.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/components/RecordCard.tsx) - 展示和编辑标签
- [pages/RecordsPage.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/pages/RecordsPage.tsx) - 标签筛选功能

---

### 5. 提醒与通知功能

**功能描述**：
- 每日占卜提醒（可自定义时间）
- 连续签到激励提醒
- 使用 Web Notifications API

**实现要点**：
- 需要用户授权通知权限
- 使用 `setTimeout` 或 Service Worker 实现
- 提供开关设置

---

### 6. 占星骰子进阶功能

#### 6.1 飞星解读
- 根据当前结果计算行星的飞星轨迹
- 如：火星在第7宫 → 飞星到第7宫的原始守护星（金星）的状态

#### 6.2 互容与接纳
- 分析行星间的互容关系（如：水星在金牛座、金星在双子座 → 互容）
- 计算接纳关系及其影响

#### 6.3 空亡与特殊度数
- 识别特殊度数（0°、13°、26°等关键度数）
- 提示是否存在空亡星座等特殊情况

---

### 7. 数据可视化增强

**新增图表类型**：
- 雷达图：展示用户个人占星能量分布
- 趋势图：展示某行星/星座的出现趋势随时间变化
- 网络图：展示行星、星座、宫位之间的关联强度

**实现方案**：
- 可引入 `nivo` 图表库（比 recharts 更丰富）
- 或继续扩展 recharts 的使用

**相关文件**：
- [components/StatsChart.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/components/StatsChart.tsx)
- [utils/statistics.ts](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/utils/statistics.ts)

---

### 8. 骰子物理引擎优化

**当前状态**：使用纯CSS动画

**优化方向**：
- 引入 `cannon-es` 或 `matter-js` 物理引擎
- 实现真实的骰子碰撞物理效果
- 支持3D场景下的真实投掷物理模拟

**替代方案**：
- 使用 `Three.js` + 物理引擎实现真3D场景
- 支持自定义观察角度

---

## 🟡 P2 - 低优先级（体验打磨）

### 9. 主题与无障碍优化

**功能描述**：
- 支持深色/浅色模式切换
- 高对比度模式
- 字体大小调节
- 完整的键盘导航支持
- 屏幕阅读器友好的 ARIA 标签

**实现要点**：
```typescript
// 主题设置接口
interface ThemeSettings {
  mode: 'dark' | 'light';
  highContrast: boolean;
  fontSize: 'small' | 'normal' | 'large';
  reducedMotion: boolean;
}
```

---

### 10. 智能解读建议

**功能描述**：
- 基于用户历史记录，分析用户的高频问题类型
- 推荐相关的知识库内容
- 提供个性化的学习路径建议

**核心算法**：
```typescript
function generateUserInsight(records: DiceRecord[]) {
  // 1. 分析高频问题类型
  // 2. 分析最常出现的行星/星座/宫位
  // 3. 识别用户的盲点（从未出现的组合）
  // 4. 生成个性化建议
}
```

---

### 11. 社交分享功能

**功能描述**：
- 生成精美的占卜结果分享图片
- 支持一键分享到社交平台
- 提供多种分享模板风格

**技术实现**：
- 使用 `html2canvas` 或纯 Canvas 生成图片
- 支持自定义背景、水印

---

### 12. 快捷键支持

**快捷键列表**：
- `空格` - 投掷骰子
- `R` - 重新投掷
- `S` - 保存当前记录
- `/` - 聚焦搜索框
- `1-4` - 快速切换投掷力度
- `ESC` - 关闭弹窗/取消操作

**相关文件**：
- [pages/RollPage.tsx](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/pages/RollPage.tsx)

---

## 🟢 P3 - 探索性功能（长期规划）

### 13. AI 智能解读

**功能描述**：
- 集成本地大语言模型（如 WebLLM）
- 提供更自然语言的解读
- 支持追问式对话，深入探讨问题

**优势**：
- 数据不上传，保护隐私
- 可定制占星风格（传统/现代/心理）

---

### 14. 多人协作模式

**功能描述**：
- 创建共享收藏集
- 邀请好友共同记录和解读
- 评论和讨论功能

**技术挑战**：
- 需要后端支持（当前是纯前端）
- 实时同步机制

---

### 15. PWA 支持

**功能描述**：
- 支持离线使用
- 可添加到主屏幕
- 后台数据同步
- 推送通知

**实现要点**：
- 配置 `vite-plugin-pwa`
- 编写 Service Worker
- 处理缓存策略

---

### 16. 时间维度增强

**功能描述**：
- 记录投掷时的占星时间（考虑真太阳时）
- 关联当前天象（行运星位置）
- 分析行运与投掷结果的关联

**数据来源**：
- 使用 `swiss ephemeris` 的 WebAssembly 版本
- 或集成公开的占星API

---

## 🔧 技术债务与代码优化

### 17. 单元测试覆盖

**建议测试范围**：
- 统计计算函数（[statistics.ts](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/utils/statistics.ts)）
- 数据转换工具函数
- 状态管理逻辑（[useDiceStore.ts](file:///Volumes/ExMac/traeProject/全站1/yq-30/src/store/useDiceStore.ts)）

**技术选型**：
- `vitest`（与 Vite 生态兼容）
- `@testing-library/react`

---

### 18. 性能监控

**功能描述**：
- 集成 Web Vitals 监控
- 记录关键性能指标（FCP、LCP、CLS）
- 可选：上报到监控平台（可配置关闭）

---

### 19. Bundle 体积优化

**当前状态**：
- recharts 体积较大（~300KB gzipped）
- 可考虑按需引入或替换

**优化方案**：
```json
// vite.config.ts 中配置
optimizeDeps: {
  include: ['recharts/es6/BarChart', 'recharts/es6/PieChart'],
  exclude: ['recharts']
}
```

**替代方案**：
- 使用更轻量的图表库如 `chart.js`
- 或自行实现基础图表组件

---

### 20. 国际化（i18n）支持

**功能描述**：
- 支持多语言切换
- 第一阶段：中文、英文
- 占星术语的精准翻译

**技术选型**：
- `react-i18next` 或 `@formatjs/intl`

**待翻译内容**：
- UI 界面文字
- 行星、星座、宫位名称
- 解读内容（工作量大，可分阶段）

---

## 📈 数据安全与隐私增强

### 21. 本地数据加密

**功能描述**：
- 可选密码保护功能
- 使用 Web Crypto API 加密存储数据
- 导出的数据也可选择加密

**实现要点**：
```typescript
// 使用 AES-GCM 加密
async function encryptData(data: string, password: string): Promise<string> {
  const key = await deriveKey(password);
  const encrypted = await crypto.subtle.encrypt(
    { name: 'AES-GCM', iv: iv },
    key,
    new TextEncoder().encode(data)
  );
  return btoa(String.fromCharCode(...new Uint8Array(encrypted)));
}
```

---

## 🎯 实施路线图建议

### 第一阶段（1-2周）
- P0: 数据备份与恢复
- P0: 性能优化（虚拟滚动）
- P2: 快捷键支持

### 第二阶段（2-3周）
- P1: 标签系统增强
- P1: 数据可视化增强
- P2: 主题与无障碍优化

### 第三阶段（3-4周）
- P0: 解读生成算法优化
- P1: 飞星解读等进阶功能
- P1: 智能解读建议

### 第四阶段（长期）
- P3: AI 智能解读
- P3: PWA 支持
- P3: 时间维度增强

---

## 💡 创新功能点子

### 占星日记
- 结合每日运势和投掷记录，生成月度占星日记
- 自动识别本月的主题和趋势

### 梦境占星
- 记录梦境，使用骰子关联梦境的占星含义
- 梦境与占星符号的对应映射

### 伴侣合盘
- 支持记录两人的出生信息
- 对比分析两人的投掷模式
- 关系主题分析

### 占卜仪式
- 自定义占卜流程（如：特定的净化步骤）
- 可选的背景音乐和环境音效
- 占卜前的专注引导（已部分实现）

---

**✨ 优化建议定期回顾和更新，愿项目越来越好！**
